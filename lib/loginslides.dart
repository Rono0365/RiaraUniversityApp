import 'package:flutter/material.dart';

class loginslides extends StatelessWidget {
  const loginslides({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}